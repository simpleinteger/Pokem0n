<p id="notice"><%= notice %></p>

<p>
  <b>Deck Name:</b>
  <%= @current_deck.name %>

  <b>  Deck Size:</b>
  <%= @cards_in_deck.size %>

</p>

<%= render 'formCards' %>
<br>

<%= @cards_in_deck.each do |card| %>
  <%= "Question: #{card.question} => " %>
  <%= "Answer: #{card.answer}  " %>
  <%= link_to 'edit card', "/decks/#{@current_deck.id}/card/edit/#{card.id}" %>
  <%= link_to 'delete card', "/decks/#{@current_deck.id}/cards/#{card.id}", :method => :delete %> 

<br>
<% end %>

<br>
<%= link_to 'Back', decks_path %>
